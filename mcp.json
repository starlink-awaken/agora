{
  "mcpServers": {
    "agora": {
      "command": "agora",
      "args": ["mcp"],
      "description": "Agora v1.5 — 14 MCP tools: registry, multi-protocol routing (MCP/REST/gRPC/WS), proxy, pipeline orchestration, event bus"
    }
  }
}
